window.docContents[33].resolve({"/#apc-optimizer___-A-Verified-Constraint-System-Optimizer--VM-instantiations--OpenVM":{"contents":" runs over the BabyBear field. Its bus map assigns each bus id a type — the\nexecution bridge and memory (stateful), plus range-checker, bitwise/XOR, and PC-lookup tables\n(stateless):\n\nThe OpenVM bus types that appear in the default bus map.A instruction should receive a (pc, timestamp) and send updated values to the bus.To access a memory cell, instructions should:1. Receive a (address_space, pointer, data... (4 bytes), prev_timestamp) tuple2. Send a (address_space, pointer, data... (4 bytes), current_timestamp) tuple.Lookup to get the instruction flags from the current pc. Format: (pc, flags...)Check that a value is in a certain range. Format: (value, bits)Check that a value is in a certain range. Format: (x, y, z, op) where op is either\n0 (range check) or 1 (xor). The bus checks that x and y are bytes, and either\nop = 0 ∧ z = 0 (range check) or op = 1 ∧ z = x ^ y (xor).Check that a tuple of two values is in a certain range. Format: (x, y) where\nx < size1 and y < size2.\n\nThe two opaque predicates of the semantics are given concretely. violates encodes each lookup\ntable (byte and XOR checks, range widths, lookup arities); breaksInvariant encodes the soundness\ninvariant that data written to the register / main-memory address spaces is byte-ranged.\n\nWhether a message conflicts with the lookup table of the bus it is sent on.\n\nWhether a message breaks an invariant on which soundness depends.\n\nThese assemble into the audited OpenVM semantics, with memory using OpenVM's send-the-new-record\nconvention:\n\nThe OpenVM bus semantics for a given bus map (default: the hard-coded default bus map).\n\n","context":"apc-optimizer: A Verified Constraint-System Optimizer\u0009VM instantiations","header":"5.1. OpenVM","id":"/#apc-optimizer___-A-Verified-Constraint-System-Optimizer--VM-instantiations--OpenVM"}});